#include "main.h"
/**
 * _abs - functiom to print the absolute value of a number
 *
 * @n: input number
 *
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
